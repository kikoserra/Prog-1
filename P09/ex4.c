#include <stdio.h>
int mdc(int x, int y);
int main(){
    int divisor, dividendo;
    printf("Qual o divisor qual o dividendo?"); scanf("%d %d", &divisor, &dividendo);
    printf("O maior divisor comum entre %d %d é %d", divisor, dividendo, mdc(divisor, dividendo));
}
int mdc(int x, int y){
    int mdc = 0;
    if(x-y)


    return mdc;
}